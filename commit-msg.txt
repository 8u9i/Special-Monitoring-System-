fix: populate student progress arrays from DB on load

GET /api/students had every progress array hardcoded to [] — only xp
was loaded from the students table. This meant all memorization detail
(which hadiths, surahs, pages, english units) was lost on page reload.

Now queries all four progress tables (student_hadiths, student_surahs,
student_surah_pages, student_english_progress) in parallel via ANY($1)
and populates memorized/review arrays correctly per student.

Icons verified: all 49 unique icon names resolve to valid Lucide v0.547.0
icons. No changes needed.

Co-authored-by: CommandCodeBot <noreply@commandcode.ai>
