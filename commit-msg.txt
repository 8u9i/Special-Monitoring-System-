feat: use lucide-angular for icons (replaces broken inline-SVG attempt)

The previous attempt used inline SVG strings with bypassSecurityTrustHtml, but
the source data was repeatedly corrupted. Switched to the official
lucide-angular package (1.0.0) which supports Angular 21 and ships 1705+
icons. The user provided the icon mapping.

- Installed lucide-angular@1.0.0
- src/app/shared/constants/icons.ts: maps our internal names to Lucide names
  (e.g. add -> plus, eco -> leaf, emoji_events -> trophy)
- src/app/shared/components/app-icon/app-icon.component.ts: now renders
  <lucide-angular> with size, color, strokeWidth, aria-label/role
- Registers only the 30 icons we use via LucideAngularModule.pick()
- All sizes, colors, and ARIA wiring preserved
- Build clean

Co-authored-by: CommandCodeBot <noreply@commandcode.ai>
