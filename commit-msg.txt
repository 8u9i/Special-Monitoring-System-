fix: split student-surah-pages catch-all route for POST vs DELETE

[...rest] catch-all requires at least one path segment, but the client
POSTs to /api/student-surah-pages with no segments. Split into:
- route.ts: handles POST (no path segments)
- [...rest]/route.ts: handles DELETE with [studentId, pageId] segments

Co-authored-by: CommandCodeBot <noreply@commandcode.ai>
